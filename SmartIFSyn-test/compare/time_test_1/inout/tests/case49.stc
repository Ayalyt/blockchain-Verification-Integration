/*
 * @source: etherscan.io
 * @author: -
 * @vulnerable_at_lines: 13, 15
 */

//pragma solidity ^0.4.25;
contract demo{
    function transfer(address from,address caddress,address[]memory _tos,uint[] memory v)public returns (bool){
        require(_tos.length > 0);
        bytes4 id=bytes4(keccak256("transferFrom(address,address,uint256)"));
         // <yes> <report>
        for(uint i=0;i<_tos.length;i++){
             // <yes> <report> UNCHECKED_LL_CALLS
            (bool success, bytes memory data) = caddress.call(abi.encode(id,from,_tos[i],v[i]));
        }
        return true;
    }
}